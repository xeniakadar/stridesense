repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.21 # then run `pre-commit autoupdate` to pin the actual latest
    hooks:
      - id: ruff
        args: [--fix]
        files: ^backend/
